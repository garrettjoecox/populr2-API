
import { UserError } from '../../utils/errors';

export const model = {
  createPerson(payload: { displayName: string, twitterHandle: string }) {

  },

  updatePerson(id:number, payload: { displayName: string, twitterHandle: string }) {

  },
};

export const controller = {
  createPerson(payload: { displayName: string, twitterHandle: string }) {

  },

  updatePerson(id:number, payload: { displayName: string, twitterHandle: string }) {

  },
};
